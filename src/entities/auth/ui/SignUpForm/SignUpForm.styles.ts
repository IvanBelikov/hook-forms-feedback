import { SxProps, Theme } from '@mui/material/styles'
import { red } from '@mui/material/colors'

const errorTypographyStyles: SxProps<Theme> = { color: red[300] }

export default { errorTypographyStyles }
